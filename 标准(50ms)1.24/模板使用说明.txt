包涵以下内容：
1.pid结构体，pid执行函数，pid初始化函数
2.LCD的划线，画点函数
3.按键外部中断（尽量让操作写在中断里，操作在外部，需清空标志位）
4.利用PIT3定时器实现的精准延时函数（PIT_delayms  PIT_delayus）
优先级低于此中断的只有外部中断	DAC等
5.各种需要定时器的操作，按照赵晓东学长的架构，全都放在PIT0中断里，函数从上到下的执行顺序即操作的优先级。
